{
  "scripts": {
    "build": "php build/export.php",
    "clean": "php -r \"(new Symfony\\Component\\Filesystem\\Filesystem())->remove(__DIR__ . '/dist'); echo 'dist cleared'.PHP_EOL;\""
  },
  "require": {
    "twig/twig": "^3.21",
    "symfony/filesystem": "^6.4",
    "symfony/finder": "^6.4",
    "symfony/asset": "^6.4"
  }
}
