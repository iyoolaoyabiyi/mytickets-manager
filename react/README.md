# myTickets Manager — React + Vite (TS) + Tailwind v4 (Stylesheet-based)

- **No inline utilities**: all styling lives in `assets/styles/app.css` using `@apply`.
- **Tailwind v4**: colors only in `:root`; everything else in `@theme`.
- **Brand color**: red (`--accent`).
- **Shared assets**: fonts, media, copy JSON in `/assets` (framework-agnostic).

## Run
pnpm i && pnpm dev  # or npm/yarn equivalents
