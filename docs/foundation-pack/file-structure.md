myticket-manager/
  README.md  <- links to each implementation + shared assets
  apps/
    react/   <- complete React app (own README, scripts)
    vue/     <- complete Vue app (own README, scripts)
    twig/    <- Twig templates + vanilla JS enhancements (own README)
  packages/
    assets/          <- wave.svg, circles, icons, fonts
    styles/          <- design tokens (CSS vars, JSON)
    utils/      <- zod schemas / plain JS build output for Twig
    mocks/           <- json-server db.json, routes, scripts